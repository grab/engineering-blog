# Staging overrides — merged with _config.yml for preview deploys.
#
# Preview URL (GitLab Pages, no custom DNS required):
#   http://engineering.pages.myteksi.net/public/engineering-blog
#
# GitHub branch (gh-pages-staging) is also updated via jgd for parity with
# production deploys; configure GitHub Pages to that branch if you prefer
# grab.github.io over GitLab Pages for staging.
url: http://engineering.pages.myteksi.net/public/engineering-blog

# Analytics and cookie consent are gated on jekyll.environment == "production"
# in _includes/ga.html and _includes/head.html (JEKYLL_ENV=staging).
